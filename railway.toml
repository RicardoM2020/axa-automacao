[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'gunicorn app:app --workers 1 --timeout 300 --bind 0.0.0.0:$PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
